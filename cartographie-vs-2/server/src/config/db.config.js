module.exports={
    url:'mongodb+srv://dbcartographie:cartographie1234@cluster0-tlaii.mongodb.net/test?retryWrites=true&w=majority'
}
// const mongoose = require('mongoose');
// require('dotenv').config()

// const MONGO_URL = `mongodb://${process.env.MONGO_USER}:${process.env.MONGO_DBPASSWORD}@${process.env.MONGO_URL}/${process.env.MONGO_DBNAME}`;


// mongoose
//   .connect(MONGO_URL, { useNewUrlParser: false, useUnifiedTopology: false })
//   .then(() => console.log('La connexion à la base de données MongoDB est établie'))
//   .catch(err =>
//     console.log(
//       `Erreur lors de la connexion à la base de données à de \n${err}`
//     )
//   );

// module.exports = db;
